## 0.1.0

- Restructured repository to package-at-root + docs site in `docs/`.
- Migrated all Heroicons Animated React icons into Flutter models.
- Added icon generation pipeline (`tool/generate_heroicons_from_react.py`).
- Added `allHeroicons` list with full migrated icon set.
